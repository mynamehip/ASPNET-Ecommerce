@model ASPNET_Ecommerce.Models.ViewModels.Auth.ResetPasswordViewModel

@{
    ViewData["Title"] = "Set New Password";
}

<div class="flex items-center justify-center min-h-[70vh]">
    <div class="w-full max-w-lg">
        <div class="bg-white rounded-2xl shadow-xl border border-gray-100 p-8">
            <div class="text-center mb-6">
                <div class="inline-flex items-center justify-center w-14 h-14 bg-brand-100 rounded-full mb-4">
                    <svg class="w-7 h-7 text-brand-600" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 15v2m0 4a9 9 0 100-18 9 9 0 000 18zm-3-8a3 3 0 116 0c0 1.306-.835 2.417-2 2.83V15a1 1 0 11-2 0v-1.17A2.994 2.994 0 019 11z"/></svg>
                </div>
                <h1 class="text-2xl font-bold text-gray-900">Choose a new password</h1>
                <p class="text-gray-500 mt-1">Complete the recovery flow by confirming a new password.</p>
            </div>

            <form asp-action="ResetPassword" method="post" class="space-y-5">
                <input asp-for="Token" type="hidden" />

                <div asp-validation-summary="ModelOnly" class="text-red-600 text-sm bg-red-50 rounded-lg p-3"></div>

                <div>
                    <label asp-for="Email" class="block text-sm font-medium text-gray-700 mb-1"></label>
                    <input asp-for="Email" readonly class="block w-full rounded-lg border border-gray-300 bg-gray-100 px-4 py-2.5 text-sm text-gray-600" />
                    <span asp-validation-for="Email" class="text-red-500 text-sm mt-1"></span>
                </div>

                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                    <div>
                        <label asp-for="Password" class="block text-sm font-medium text-gray-700 mb-1"></label>
                        <input asp-for="Password" autocomplete="new-password"
                               class="block w-full rounded-lg border border-gray-300 bg-gray-50 px-4 py-2.5 text-sm focus:border-brand-500 focus:ring-brand-500 transition" />
                        <span asp-validation-for="Password" class="text-red-500 text-sm mt-1"></span>
                    </div>
                    <div>
                        <label asp-for="ConfirmPassword" class="block text-sm font-medium text-gray-700 mb-1"></label>
                        <input asp-for="ConfirmPassword" autocomplete="new-password"
                               class="block w-full rounded-lg border border-gray-300 bg-gray-50 px-4 py-2.5 text-sm focus:border-brand-500 focus:ring-brand-500 transition" />
                        <span asp-validation-for="ConfirmPassword" class="text-red-500 text-sm mt-1"></span>
                    </div>
                </div>

                <button type="submit"
                        class="w-full py-2.5 px-4 text-sm font-semibold text-white bg-brand-600 rounded-lg hover:bg-brand-700 focus:ring-4 focus:ring-brand-200 transition">
                    Update password
                </button>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}